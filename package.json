{
  "name": "smart-tracker-monorepo",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix frontend",
    "install": "npm install --prefix frontend",
    "start": "npm run dev --prefix frontend"
  }
}
